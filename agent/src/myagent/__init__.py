"""사내 클라우드 기반 AI 에이전트.

설계 원칙 세 가지:
  1. 로컬과 App Service가 **같은 코드**를 돌린다. 달라지는 것은 환경변수뿐이다.
  2. 환경변수는 config.py 에서만 읽는다. 다른 파일에 os.getenv 를 두지 않는다.
  3. 에이전트 로직(steps.py)은 인자를 받아 값을 돌려주는 순수 함수다.
     그래야 Spyder에서 한 단계씩 실행하며 변수 탐색기로 확인할 수 있다.
"""

__version__ = "0.1.0"
